# Sleep duration (in secs) between checks for updates; default=300
TRACKER_UPDATE_INTERVAL_SECS = 60 * 5
# HTTP GET timeout (in secs), must be smaller than update interval; default=10
TRACKER_TIMEOUT_SECS = 10
